<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini whatsapp</title>
    <link rel="stylesheet" href="/style.css"/>
</head>
<body>
    <h1 class="header">All chats </h1>
    <form method="get" action="/chats/new">
        <button>Add new Chat</button>
    </form>
    <br/>
    <%for(let chat of chats){%>
        <div class="chats">
            <h3>From:<i><%=chat.from%></i></h3>
            <div class="msg"><%=chat.msg%></div>
            <h3>Received by:<i><%=chat.to%></i></h3>
            <hr/>
            <p>Time:<%=chat.created_at.toString().split(" ")[4]%></p>
            <p>Date:<%=chat.created_at.toString().split(" ").slice(0,4).join(" ")%></p>
            <form method="get" action="/chats/<%=chat._id%>/edit">
                <button class="btn">Edit msg</button>
            </form>
            <br/>
            <form method="post" action="/chats/<%=chat._id%>/?_method=DELETE">
                <button class="btn">Delete msg</button>
            </form>
            
        </div><hr/>
      
    <%}%>
    <br/>
  
</body>
</html>
